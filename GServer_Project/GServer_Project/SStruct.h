#pragma once
#include <string>

// -----------------------------------------------------------------------
// SStruct.h
//   서버 내부에서 사용하는 공용 데이터 구조체 모음.
//   DB 조회 결과, 서비스 간 데이터 전달 등에 사용한다.
//   DataBase.cpp 의 extern 글로벌 변수(p_x, p_y, ...) 를 대체한다.
// -----------------------------------------------------------------------

// -----------------------------------------------------------------------
// DB 입력용 구조체
// -----------------------------------------------------------------------
struct spAddPlayer
{
    int x = 0;
    int y = 0;
    int hp = 0;
    int exp = 0;
    std::string name;
};

struct spUpdatePlayer
{
};

// -----------------------------------------------------------------------
// DBPlayerData
//   Load_DB() 가 DB에서 읽어온 플레이어 정보를 반환할 때 사용하는 구조체.
//   기존 extern SQLINTEGER p_x, p_y, ... 전역 변수 방식을 대체한다.
// -----------------------------------------------------------------------
struct DBPlayerData
{
    bool    success = false;    // DB 조회 성공 여부

    int     id      = 0;
    int     x       = 0;
    int     y       = 0;
    int     hp      = 0;
    int     maxhp   = 0;
    int     exp     = 0;
    int     level   = 0;
    char    name[50]= {};       // 로그인 이름 (NAME_LEN=50)
};
